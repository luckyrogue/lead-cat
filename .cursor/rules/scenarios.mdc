---
description: Scenario engine and editor
globs: "{backend/internal/domain/scenario/**,backend/internal/platform/scenario/**,frontend/src/shared/presets.ts}"
alwaysApply: false
---

# Scenarios

- Definition JSON: `nodes[]`, `edges[]` — see `docs/SCENARIOS.md`.
- Presets in Go (`backend/internal/domain/scenario/presets.go`) and TS (`frontend/src/shared/presets.ts`) must stay aligned.
- New node types: domain validate + executor + (optional) editor UI.

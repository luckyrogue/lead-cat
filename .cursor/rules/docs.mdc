---
description: Documentation sync
globs: "{docs/**,*.md,deploy/**}"
alwaysApply: false
---

# Docs

When changing API, env, auth, or deploy — update `AGENTS.md`, `deploy/README.md`, or `deploy/.env.example`.

Index: `docs/README.md`.
